{
  "name": "nest-fintech-kafka",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo root. Holds git hooks (husky) for the fintech services.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.11"
  }
}
